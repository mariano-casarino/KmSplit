export interface User {
  id: number;
  /** Apodo: el nombre que se muestra en toda la app (integrantes, cargas, resumen). */
  name: string;
  first_name: string;
  last_name: string;
  email: string;
  created_at: string;
}
